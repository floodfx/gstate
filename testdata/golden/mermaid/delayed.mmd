---
config:
  theme: default
  themeVariables:
    fontSize: 16px
---
flowchart TB
    __start(("●"))
    __start --> idle
    idle(["idle"])
    timeout(["timeout"])
    idle -->|"after 100ms"| timeout
